{
  "name": "native-cypher",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "prebuild": "rm -rf {build,prebuilds}",
    "build": "prebuildify --quiet --napi",
    "install": "node-gyp-build \"sh prebuild.sh\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.5.0",
    "nan": "^2.14.0",
    "node-addon-api": "^1.6.2",
    "node-gyp-build": "^4.1.0",
    "os-name-cli": "^1.0.0"
  },
  "devDependencies": {
    "chokidar-cli": "^1.2.2",
    "prebuildify": "^3.0.0"
  }
}
