{
  "name": "native-cypher",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "gypfile": true,
  "binary": {
    "napi_versions": [
      2,
      3
    ]
  },
  "scripts": {
    "prebuild": "rm -rf {build,prebuilds}",
    "build": "prebuildify --napi",
    "install": "prebuild-install || node-gyp-build \"sh prebuild.sh\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tung Vu <me@tungv.com>",
  "repository": {
    "type": "git",
    "url": "git@github.com:tungv/native-cypher.git"
  },
  "license": "ISC",
  "dependencies": {
    "node-addon-api": "^1.6.2",
    "node-gyp-build": "^4.1.0",
    "prebuild-install": "^5.3.0"
  },
  "devDependencies": {
    "chokidar-cli": "^1.2.2",
    "prebuildify": "^3.0.0"
  }
}
